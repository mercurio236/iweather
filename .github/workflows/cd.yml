name: CD

on:
  push:
    branches:
      - main

jobs:
  CD:
    name: Continuous Delivery
    runs-on: ubuntu-latest

    steps:
      - name: checkout repository
        uses: actions/checkout@v4
        
